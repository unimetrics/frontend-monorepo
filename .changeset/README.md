# Changesets

This directory stores pending release notes for the monorepo.

Create a changeset:

```sh
pnpm changeset
```

Apply version updates from queued changesets:

```sh
pnpm changeset:version
```
